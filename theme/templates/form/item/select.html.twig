{{ item.label }}
<select id="{{ attributes.name }}" {{ html_attributes(attributes) }}>
  {% for params in options %}
    <option {{ html_attributes(params.attributes) }}>{{ params.label }}</option>
  {% endfor %}
</select>
{{ item.description }}
